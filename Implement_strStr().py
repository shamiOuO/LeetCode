def strStr(self, haystack: str, needle: str) -> int:
    if needle == "":
        print(0)
    count =0
    if needle in haystack:
        print(needle)
    else:
        print(-1)


strStr(self = '',haystack = 'aaaaa',needle = 'bba')